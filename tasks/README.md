# tasks/

Contains `TASKS.md` — a single running list of open commitments.

Claude PROPOSES additions; you CONFIRM them. Claude never silently adds, completes, or deletes tasks. Status markers: `[ ]` open, `[x]` done (you mark these), `[PROPOSED]` awaiting your confirmation.
